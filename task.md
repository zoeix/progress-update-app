- Only run command I provided

1. Run command

```bash
.venv/bin/python app.py --vscode
```

2. Run command and wait after it shows 'Done'
```
.venv/bin/python bridge/hook.py
```

3. read bridge/prompt.md, and save the output as bridge/res.json
